#include <iostream>
#include <vector>

using namespace std;
int main(){
    int N; cin >> N;
    int K; cin >> K;
    vector<int> A(N);
    for(int i = 0; i < N; i++){
        cin >> A[i];
    }
    int index = 0;
    while(index > N){
        if()
    }
}